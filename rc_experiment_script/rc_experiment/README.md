# rc_experiment Package Description

This is a package used in `Experiment.py` script. 

---

It contains the components of all the major steps in a reverse curse experiment for a given `model_checkpoint`. 